name: video_match
description: Dating platform based on short video clips.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  camera: 0.5.7+3
  path_provider: 1.6.0
  video_player: 0.10.7
  firebase_core: 0.4.4
  cloud_firestore: 0.13.1
  firebase_storage: 3.1.1
  google_sign_in: 4.1.1
  firebase_auth: 0.15.4
  google_fonts: 0.3.9
  firebase_ml_vision: 0.9.3
  url_launcher: 5.4.2
  geolocator: 5.3.0
  
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/